# Aide 医疗健康 RAG：当前架构与后续门槛

## 本机研究版链路

```text
官方原文（66 条正式释义 + 2 篇卫健委症状科普）
  → 原文与 SHA256 核对、正文清理、68 条 JSON
  → 章节切片、BGE-small-zh-v1.5 本机向量化
  → 独立 Chroma 研究索引
  → 健康知识模式：急症 / 个人诊断及用药请求拦截
  → 查询与短追问、向量距离拒答门槛、BM25 强关键词补召回、最多 5 条证据
  → DeepSeek 官方 API 结构化生成
  → 引用编号验证、来源卡与研究阶段提示
```

聊天中的医疗路径与 Aide 原有普通助手路径分开，使用 `MEDICAL_LLM_*` 配置；不向原有第三方模型地址发送健康提问。向 DeepSeek 发送本轮问题、最多四轮截短上下文和检索证据。模型输出引用必须匹配已提供证据编号。紧急情况直接显示急救提示；无证据、个人诊断或改药请求直接拒答；模型不可用时显示错误。

研究索引允许 `source_checked` 原文核对资料，仅限 `NODE_ENV=development`；正式公共索引只允许 `clinician_reviewed` 且未过期、未撤回的资料。目前没有经过医疗专业审核的文档，正式公共索引不可用于面向公众的问答。Aide 原有聊天数据库会保存健康问题；会话列表提供删除入口，后端删除会话及消息并清除本进程缓存。外部模型服务收到的数据不在本机删除范围内。

## 当前验收结果与限制

本机手工题集 100 道：有依据 50 道、无依据 20 道、紧急 20 道、追问 10 道。当前 Recall@1 为 0.98、Recall@5 为 1.0，另外三组均通过；完整基线对照见 [评估报告](backend/medical/eval_report_research.md)。此题集参与了距离门槛和规则调整，结果不能代表真实用户表现。已通过一次 Aide WebSocket 端到端测试：普通科普给出证据引用，胸痛伴冷汗进入急救分支，天气问题拒答。生成文字仍可能出现证据不支持的推断；引用编号校验只证明引用存在，尚未自动验证语义。

下一阶段需要扩充独立且更困难的评估集，做错别字、口语、混合意图和多轮追问测试；由医疗背景人员核对资料、回答、紧急边界和适用人群；核查来源使用条件与隐私方案，再讨论公开上线。

执行命令、配置和测试输入见 [医疗模块说明](backend/medical/README.md) 与 [本机测试指南](medical_test_guide.md)。
