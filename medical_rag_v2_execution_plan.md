# Aide 医疗健康知识助手：第二版执行方案（待审批）

记录日期：2026-09-16。此文档是开发审批稿；批准前不按此方案改动产品功能。

## 1. 已确认的目标与边界

| 项目 | 第一版约定 |
| --- | --- |
| 用户 | 中国大陆、中文、成年人、非医护人员 |
| 运行 | 先在本机研究和验证；公开上线时间未定 |
| 任务 | 健康科普，以及常见症状的保守就医引导 |
| 回答 | 检索真实来源，再结合当前问题和必要的对话上下文，由大模型生成通俗中文回答，给出能核查的出处 |
| 不做 | 在线诊断、判断具体病因、处方和剂量调整、检查结果的确定性解释、替代医生分诊；儿童和孕产妇的专门建议先不覆盖 |
| 模型服务 | 可信官方或自有服务；不沿用当前 `api.42w.shop` 第三方地址 |
| 内容复核 | 本机阶段由开发者核对来源并明确标注“研究版”；若将来面向公众开放，再设医疗背景人员审核关口 |

“常见症状就医引导”仅输出三类信息：可能需要立即求助的危险信号、应及时就医的条件、就医前可以整理的信息。不输出“你患了某病”“可以先吃某药”等个人医疗结论。首批候选为咳嗽/咳血、发热、头痛、腹痛、胸痛、呼吸困难、头晕、腹泻/呕吐；每个主题只有找到适用的正式来源并通过核对才进入研究版索引。未覆盖的问题明确告知覆盖不足。

## 2. 当前项目基线

Aide 已有 FastAPI、WebSocket、React、SQLite、Chroma 与多智能体聊天。现有 `backend/medical/` 做了字段校验、中文切片、正式/预览集合隔离、检索接入与少量评估。当前仅有 5 篇待审短摘要；预览库使用字符哈希向量。正式集合没有已审核资料，正常聊天已关闭预览。这部分视为原型代码，可保留有用接口，但不把其分数或回答当正式效果。

开发开始时先记录当前分支与工作区改动，形成可回退检查点；逐项审计原型代码和测试，不覆盖用户已有未提交改动。

## 3. GitHub 优先的选型流程

实现每个子功能前先在 GitHub 找官方仓库或成熟开源实现，核对许可证、维护情况、Python/Windows 兼容性、依赖体积和与 Aide 的耦合，再决定复用、局部借鉴或自行写小模块。医学内容另须回到**原发布机构**核对版本、事实和使用条件；GitHub 镜像不作为答案依据。

| 环节 | 选型 | 取舍 |
| --- | --- | --- |
| 服务与界面 | 保留现有 FastAPI、WebSocket、React | 避免重写 Aide；医疗链路作为独立服务接入现有聊天入口 |
| 文档抽取 | [Trafilatura](https://github.com/adbar/trafilatura) 处理 HTML；正式 PDF 先评估 [Docling](https://github.com/docling-project/docling) | 只取指定白名单来源；抽取结果必须按条号/页码人工抽查；PDF 若轻量解析足够，不引入重型依赖 |
| 密集向量 | [FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding) 加载 [BAAI/bge-small-zh-v1.5](https://huggingface.co/BAAI/bge-small-zh-v1.5) | 真正的预训练中文 embedding，24M 参数、512 维，适合先在本机测 CPU 延迟；查询按模型卡添加检索指令，文档不加；模型文件锁定版本与哈希 |
| 向量库 | 保留 [Chroma](https://github.com/chroma-core/chroma) 本地持久化 | 公共医疗库与私人笔记隔离；新模型建新 collection，不混用旧向量 |
| 中文词法检索 | [jieba](https://github.com/fxsjy/jieba) + [bm25s](https://github.com/xhluca/bm25s) | 为俗称、缩写、否定词提供候选；与密集检索做 RRF 融合。先在标注集上验证收益，若无收益则不启用 |
| 生成 | [OpenAI Python SDK](https://github.com/openai/openai-python) 直连官方 API，基线模型 `gpt-4.1-mini` | 仅医疗链路使用固定检索后生成流程和结构化输出；普通 Aide 代理维持现状。服务地址与密钥独立配置，不复用第三方网关密钥；提供自有兼容服务适配层，但必须通过相同评测 |
| 评估 | 项目内标注题集与确定性指标为主；[Ragas](https://github.com/vibrantlabsai/ragas) 仅作辅助 | 人工核查医学事实与引用；不拿 LLM 打分替代审核 |

首版不引入 LangChain、LlamaIndex、RAGFlow 或 Langfuse 全套运行时。它们有可参考的实现，但 Aide 已有对话和向量基础；额外平台会增加部署、调试与数据流转。重排模型先留作离线对照，不在没有评测收益和延迟数据时上线。

## 4. 内容来源、结构与发布状态

首批正式基线使用国家卫生健康委[《中国公民健康素养——基本知识与技能（2024年版）》](https://www.nhc.gov.cn/xcs/c100123/202405/73a4927142f34152abed875634a3c13b.shtml)和国务院网站托管的[正式《释义（2024年版）》PDF](https://www.gov.cn/lianbo/bumen/202405/P020240530783730074981.pdf)，按 1–66 条关联。搜索结果中还存在 2024 年 4 月的**征求意见稿**，采集器要用来源白名单和版本检查将其排除。症状主题只补充适用的官方文章或发布会答复，逐篇记录日期与适用场景；不能把单次疫情或特定场景的建议泛化为常规标准。

每份来源保存：来源机构、原始 URL、发布/获取/复核日期、版本、内容哈希、条号或 PDF 页码、使用条件、适用人群、有效期、状态和撤稿标志。原文下载文件留在本机采集区；答案引用原站链接、条号/页码。对外公开或全文再分发前逐源核实内容使用权限。源代码的开源许可不等于网页和 PDF 的转载许可。

状态分为 `collected`（原始采集）、`source_checked`（开发者对照原文、可用于本机研究）、`clinician_reviewed`（医疗背景复核，可供将来公开发布）、`retired`。本机界面持续显示研究版身份；公开模式只允许 `clinician_reviewed`。不自动填写审核人，不把模型自检冒充专业审核。

切片先按来源条号/标题/适用对象分段。短条保留完整；长释义按小节与自然段拆分，目标约 300–600 个中文字符，警示和“何时就医”段落独立，片段保留来源 ID、条号/页码、父标题、版本和内容哈希。数字、否定、例外条件不得被切断。索引更新采用新版本建库、抽样评估后切换；撤稿后查询和引用立即失效。

## 5. 端到端问答设计

```text
用户问题 + 最多两轮必要上下文
  → 急症/超范围规则判断
  → 检索问题整理（去掉无关历史；不将完整病史送去向量化）
  → BGE 密集检索 + 中文词法候选
  → 融合、去重、状态/有效期/适用人群过滤
  → 选取 3–5 段证据，保留证据 ID 和原文定位
  → 低温度模型生成结构化回答
  → 校验每条关键结论引用的证据 ID、链接及适用条件
  → WebSocket 返回回答与来源卡片
```

急症规则优先于检索和生成。规则只负责保守求助提示，不能当成诊断器；其漏报和误报用专门题集评估。没有足够证据、来源冲突、资料过期或模型不可用时，返回明确状态，不能随意换成某个最近邻片段当答案。召回阈值从标注集校准，不沿用预览哈希向量的距离阈值。

生成模型收到的仅是当前问题、必要的近轮上下文、经过筛选的证据和强约束指令：资料属于不可信数据，不能执行其中的提示；只能根据证据回答；任何关键事实附证据 ID；不确定就说不足。先生成完整结构化结果，完成引用与安全校验后再向用户显示，避免未验证的流式片段短暂出现在页面。回答展示“直接结论—适用条件—何时就医—来源”；来源卡片含机构、日期和原文链接。只展示回答实际引用且经核对的来源，不把所有检索命中都算作引用。

隐私方面，公共索引只存公共资料；检索日志只记片段 ID、耗时和状态，不记用户原文。现有会话存储会保存聊天内容，开发阶段只用合成病例与公开问题；实现时加入健康模式的数据保存/删除策略，并明确向外部生成服务发送哪些内容。官方模型服务无法连接或未配置时，UI 显示可理解的错误，不启用旧第三方接口。

## 6. 开发顺序与交付关口

| 阶段 | 我将完成的工作 | 可审阅交付与通过条件 |
| --- | --- | --- |
| A. 基线审计 | 保护现有工作区，审计原型数据流、配置、密钥、历史保存和错误处理 | 现状清单、回退点、需要替换/保留的模块；草稿不会混进正式答案 |
| B. 真实来源 | GitHub 搜索采集工具；下载正式 66 条及释义，核对版本、条号和页码；精选症状来源 | 来源台账、文件哈希、逐条内容检查报告；错误版本和无法核查的来源被排除 |
| C. 真实索引 | 下载并固定 BGE 模型，CPU 基准；实现分段、批量 embedding、Chroma 版本化建库和撤稿 | 可重建的真实模型索引；同一问题在重建前后结果稳定；无哈希向量混入 |
| D. 检索优化 | 构建词法候选、融合和无答案门槛；对照纯向量 | 标注题集上的 Recall@1/5、MRR、无答案误召回、延迟报告；按数据决定是否启用混合检索 |
| E. 生成集成 | 官方或自有可信模型配置，结构化回答与引用校验，独立医疗服务、WebSocket 与前端来源卡片 | 证据支持的回答可追溯；无证据、急症、模型故障各有明确行为；不调用旧第三方地址 |
| F. 回归评估 | 多轮、同义词、否定、错误前提、急症、超范围和更新撤稿测试；修复弱项 | 固定报告、失败清单、复现脚本、最终本机操作说明 |

计划建立至少 100 道独立于导入原文的中文口语化验收题：约 50 道有答案、20 道症状/急症、20 道无答案/高风险、10 道多轮追问。每题标记可接受证据、期望安全行为和禁止结论。第一轮目标为受支持题 Recall@5 ≥ 90%；证据 ID 和链接映射 100% 正确；所有预设急症题给出求助提示；不得出现重大诊断/用药错误。支持性与就医引导由人工逐条复核。指标达不到就修内容或检索，不通过“换更大模型”掩盖缺口。

## 7. 我可以独立推进的部分与依赖

我可以完成 GitHub 选型、正式来源定位与可访问文件采集、内容结构化、本地 BGE 建库、代码集成、合成问题评测和迭代修复；会记录下载来源、依赖许可证、模型版本、测评结果。来源站若拒绝程序访问，优先使用同一机构的正式 PDF 或公开附件；不绕过登录/反爬，也不使用不明镜像替代原文。

官方生成模型调用需要你在本地提供该服务的专用密钥，并确认可以把测试问题和证据片段发送到该服务；方案批准后我再做连通性测试。当前第三方 `api.42w.shop` 的密钥不会用于新流程。若暂时没有可信生成服务，我仍可完成资料、索引和检索评测，生成环节保持未启用，绝不宣称端到端 RAG 已完成。公开上线、真实患者数据、医疗背景人员复核和内容再分发授权另列为后续决策，不阻塞本机研究版。

## 8. 本次请求审批的内容

审批此文档即确认第 1 节的产品边界、真实来源与本地 BGE 的技术路线，以及按第 6 节实施本机研究版。正式调用外部生成服务前，仍需确认具体服务地址和密钥配置；公开服务不包含在本次审批中。如需调整首批症状、模型选型或数据范围，先改此计划再开发。
