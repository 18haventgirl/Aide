# Aide 医疗健康个人助手：首版实现方案

## 1. 产品定位与首版边界

面向非医护人员，先做**健康知识解释与就医准备助手**：回答常见健康科普问题，解释通用术语，帮助用户整理向医生提问的要点。回答采用通俗中文，标明来源、发布日期或审核日期，以及内容适用范围。

首版不提供疾病诊断、个人治疗决策、处方或剂量调整、检验结果的确定性解读。对急症线索、孕产妇和儿童等特殊人群，以及药物相互作用等高风险问题，优先给出求助与就医建议，不生成个体化医疗结论。紧急情况提示尽快联系当地急救服务；此类判断不只依赖模型生成。

首批覆盖约 30–50 个经过人工核对的知识主题：健康素养、常见症状的基础解释、慢病日常管理常识、预防与筛查、就医准备。优先选择稳定、面向公众的内容，暂不收录需要频繁更新的流行病动态、药价与医院服务信息。

## 2. 当前项目基础与缺口

项目已具备 FastAPI、React 聊天界面、WebSocket 流式对话、Agent 路由、SQLite 和 Chroma。`backend/core/vector_core` 可存储和查询文本，但集合按 `user_id` 隔离，当前主要供用户笔记使用；`backend/agent/personal_assistant_manager.py` 创建的代理尚无医疗代理或知识检索工具；`backend/api/websocket_api.py` 的对话入口直接进入 Triage Agent。

因此，首版应新建**公共、只读的医疗知识集合**，与用户私有笔记集合分开，并让医疗问答在进入生成前完成检索。不要把公共知识复制到每个用户的集合，也不要把用户病史写进公共向量库。医疗对话应有独立路由和回答规则，避免由天气、新闻或菜谱代理处理。

## 3. 首版架构

```text
用户提问
  → 医疗意图与风险分流
      → 急症/高风险：固定安全流程与就医提示
      → 普通科普：查询改写（必要时）
          → 公共知识库检索（Chroma）
          → 过滤未审核/过期内容，去重并选取证据
          → 基于证据生成通俗回答
          → 引用校验与安全检查
          → WebSocket 返回回答、来源与反馈入口
```

具体落点：

- `backend/medical/`：内容模型、导入、切片、检索、风险分流、回答组装和评估。先用普通 Python 服务调用，暂不增加 MCP 服务。
- `backend/core/vector_core/`：复用 Chroma 客户端的嵌入和存储思路，但为公共知识建立独立 collection；同时保留 SQLite 中的文档台账和审核状态。
- `backend/agent/personal_assistant_manager.py`：增加医疗代理/路由说明。医疗回答前由服务层执行检索，把有限证据作为上下文传给代理，不能只靠模型自行决定是否调用检索工具。
- `backend/api/websocket_api.py`：在现有消息入口做风险与领域分流；流式完成消息携带结构化 `citations` 和 `safety_level`。UI 在 `ui/src/components/Chat.tsx` 展示来源卡片、更新时间和“回答有问题”反馈。
- `backend/api/medical_api.py`：仅提供受保护的知识管理接口或本地导入命令；普通用户没有知识写入权限。

首版可先使用现有模型和 embedding 配置。由于医疗内容对准确性更敏感，生成温度建议单独设低值，例如 0–0.2。模型、embedding、切片版本应记录在索引元数据中，换模型时重建索引。

## 4. 知识库内容与治理

### 4.1 内容来源

中文首选国家卫生健康委的《中国公民健康素养——基本知识与技能（2024年版）》及其释义，再按主题补充权威卫生机构面向公众的材料。跨国资料可用于参考，但应核对中国的医疗语境、适用人群与更新情况。只导入明确允许使用的正文；版权或许可不明时，保存摘要、元数据与原文链接，避免整页抓取入库。每条内容经人工审核后才发布。

### 4.2 文档台账

以 `medical_document` 为主记录，至少包含：`doc_id`、`title`、`topic`、`audience`、`source_org`、`source_url`、`published_at`、`reviewed_at`、`next_review_at`、`license_note`、`language`、`status(draft/reviewed/retired)`、`version`、`reviewer`。内容正文可以保存为受版本管理的 Markdown/JSON 文件，SQLite 保存台账及导入记录。

每个切片记录 `chunk_id`、`doc_id`、`version`、`section_path`、`text`、`chunk_index`、`content_hash`、`risk_tag`。`chunk_id` 可由文档 ID、版本和内容哈希确定，以支持幂等更新。检索只允许 `status=reviewed` 且未过期的文档；撤稿或更新时同步移除旧切片。

### 4.3 内容模板

一个主题优先写成独立短文，结构为：一句话结论、适用对象、基础解释、日常可做的事、何时应就医、特殊人群注意、来源。保留原始标题与章节路径，让每个切片有足够语境。不要把不同疾病、不同适用人群或相反的建议混在同一切片。

## 5. 切片与索引策略

首版使用**按标题和语义段落切片**：先按文档/二级标题分段，再合并同一小节中相邻段落；单片约 300–600 个中文字符，必要时允许约 50–100 字重叠。短小完整的小节不强制切开；表格、警示框和“何时就医”段落独立成片并保留标题。对过长段落按句号、问号、分号等中文标点拆分。

当前 `chunk_text()` 按字符长度及英文句号寻找断点，适合作为通用工具，但不宜直接用于中文医疗文本；应新增医疗专用切片器及少量边界样例校验。首次入库仅索引审核后的内容，记录 embedding 模型名和索引版本。

## 6. 检索与回答流程

1. 从最新问题和必要的上一轮上下文提取完整检索问题；不把整段对话和用户个人信息直接送入嵌入接口。
2. 在公共医疗 collection 检索候选 8–12 片；按文档状态、更新时间、主题与适用人群过滤，再去重，送给模型约 3–5 片。首版先做向量检索；如果症状俗称、药名和专业术语召回不稳，再加关键词/BM25 混合检索。
3. 对证据片编号，生成时要求每个可核查的医学事实关联证据编号。检索文本仅视为资料，不执行其中的指令。
4. 回答生成后检查引用编号确实存在，来源 URL 对应文档台账；无合格证据时明确说明知识库未覆盖，避免给出具体医疗结论。不能把“向量距离”简单当成医学可信度，阈值需经样本评估后再定。
5. 回答格式：直接回答 → 简短依据和适用条件 → 需要就医的情况（如相关）→ 来源卡片。对健康知识用日常用语解释术语，不用未经证据支持的绝对化措辞。

高风险分流应有可测试的规则层：维护急症线索与需要人工/医疗服务处理的场景清单，结合模型分类，但不能由单一关键词命中就下确定诊断。出现可能危及生命的表现时优先提示尽快求助，不继续走普通 RAG 问答。每次分类与最终输出记录审计信息，但日志不得包含不必要的完整健康隐私。

## 7. 接口与前端最小改动

普通聊天接口沿用现有 WebSocket。完成事件增加：

```json
{
  "answer": "…",
  "safety_level": "general|caution|urgent",
  "citations": [
    {"doc_id": "health-literacy-001", "title": "…", "source_org": "…", "url": "https://…", "reviewed_at": "2026-…"}
  ],
  "knowledge_status": "grounded|insufficient|not_applicable"
}
```

UI 首版增加“健康知识”入口或明确的医疗问题路由；每条医疗回答下展示可点击来源、审核日期、适用提示和反馈按钮。急症提示应以醒目但平实的样式显示，避免把免责声明堆在每条回答开头。反馈只收集必要字段：问题类别、回答 ID、问题类型和可选说明。

## 8. 交付顺序与验收

| 阶段 | 交付物 | 完成标准 |
| --- | --- | --- |
| 1. 内容基线 | 30–50 个主题、来源清单、审核记录 | 每条可追溯、可撤回；无未经授权的全文复制 |
| 2. 知识库 | 文档台账、中文切片器、幂等导入、公共 collection | 更新/撤稿后旧片不再可检索；私有笔记与公共知识隔离 |
| 3. 问答链路 | 医疗分流、检索、回答、结构化引用 | 普通健康问题可稳定展示来源；无证据时不编造出处 |
| 4. 安全与体验 | 高风险流程、UI 来源卡片和反馈 | 急症样例优先给出求助建议；特殊人群问题不作个人化治疗建议 |
| 5. 评估发布 | 固定评测集、人工复核记录、问题清单 | 达到下述门槛后进行小范围内测 |

建议建立约 80–120 条真实口语化问题的评测集，覆盖：常见科普、同义词、复杂多轮、无答案、错误前提、急症、孕产妇/儿童、药物与隐私请求。每题标注预期证据、风险级别、可接受回答要点。首版发布门槛可设为：高风险样例无漏报；所有展示的引用均能打开且支持对应事实；普通问题的证据召回率和回答正确性由人工抽检达到事先约定目标，例如 90% 以上。具体阈值在样本集建立后锁定，避免用随意的相似度分数代替质量评估。

## 9. 后续优化路径

根据评测错误先优化内容，再优化检索：补俗称/专业术语词表和关键词混合检索；按需要加入轻量重排；针对同主题不同人群做更严格的元数据过滤；建立定期复审和失效提醒。只有当首版引用、风险分流和内容治理稳定后，再考虑上传检验报告、个人健康档案、长期记忆或主动提醒。这些功能涉及更高的隐私和医疗风险，需要另立设计与评估。

## 参考依据

- 国家卫生健康委：[中国公民健康素养——基本知识与技能（2024年版）](https://www.nhc.gov.cn/xcs/c100123/202405/73a4927142f34152abed875634a3c13b.shtml)；[释义](https://www.nhc.gov.cn/xcs/c100122/202405/f251e896a50a49ff8c632b4da93126.shtml)。
- WHO：[大语言模型在健康领域的伦理与治理指导](https://www.who.int/news/item/18-01-2024-who-releases-ai-ethics-and-governance-guidance-for-large-multi-modal-models)：强调错误、不完整、偏见和隐私风险。
- CDC：[面向公众的健康信息简明语言指南](https://www.cdc.gov/health-literacy/php/develop-materials/plain-language.html)。
- MedlinePlus：[健康信息质量评估](https://medlineplus.gov/evaluatinghealthinformation.html)；[内容使用规则](https://www.medlineplus.gov/about/using/usingcontent/)。
